# without the protocol (https://), also keep in mind /{token} will be added to this
SERVER: str = "localhost:31337/whatever"

# tokens
LADDER_BOT_TOKEN: str | None = "something"
CAT_MACRO_BOT_TOKEN: str | None = "something else"

# numeric account ids, not usernames
ADMINS: tuple[int] = (123, )
